<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
   
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">

    <%= stylesheet_link_tag "application"%>
    
   

 

    <title>Snag it!</title>
  </head>

  <body>
      <nav class="navbar navbar-expand-lg navbar sticky-top navbar navbar-dark bg-dark">
          <% if logged_in? %>
            <a class="navbar-brand" href=<%= requests_path %>>SNAG</a>
          <% else %>
            <a class="navbar-brand" href=<%= log_in_path %>>SNAG</a>
          <%end%>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href=<%= beers_path %>>Beers</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href=<%= new_request_path %>>Request a Beer</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href=<%= requests_path %>>Snag a beer</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href=<%= leaderboards_path %>>Leaderboards</a>
              </li>
              <% if logged_in? %>
                <li class="nav-item">
                  <a class="nav-link" href=<%= log_out_path %>>Logout</a>
                </li>
              <%else%>
                <li class="nav-item">
                  <a class="nav-link" href=<%= sign_up_path %>>Login/Sign Up</a>
                </li>
              <%end%>
            </ul>
          </div>
      </nav>

      
    <div class="margins">
      <% if flash.any? %>
        <% flash.each do |type, message| %>
          <ul>
            <% if type == "error" || type == "warning" || type == "notice" %>
              <li style="color: red"><%= message %></li>

            <% elsif type == "success" %>
              <p style="color: green"><%= message %></p>
            <% else %>
              <p><%= message %></p>
            <% end %>
          </ul>
          <% end %>
        <% end %>
        <% flash.delete(:error) %>
    </div>


    <%= yield %>
  </body>
</html>

<!doctype html>
